<% if obj.errors.any? %>
  <h2><%= pluralize(obj.errors.count, "error") %>  prohibited from being saved!</h2>
  <ul>
    <% obj.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>